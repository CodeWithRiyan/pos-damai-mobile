package-manager=pnpm
public-hoist-pattern[]=*

# Use hoisted linker to fix IDE resolution and native binding issues
# This creates a standard node_modules structure compatible with Electron-based IDEs